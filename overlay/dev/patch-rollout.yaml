apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: demo-rollout
spec:
  template:
    spec:
      containers:
      - name: demo
        image: nginx:1.26.0  # dev environment image
